export function parseClassification(protocol) {
    return [{
        stewardOrg: {name: 'PhenX'},
        elements: [{
            name: protocol.domainCollection,
            elements: []
        }]
    }];
}
