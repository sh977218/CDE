db.getCollection('dataelements').update({"archived" : null},{$set:{archived:false}},{"multi" : true  // update only one document });
db.getCollection('forms').update({"archived" : null},{$set:{archived:false}},{"multi" : true  // update only one document });