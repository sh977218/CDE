import { Elt } from 'shared/models.model';

declare function getEnvironmentHost(config: any, host: string): string | undefined;
declare function getEnvironmentOrigin(config: any, url: string): string | undefined;
