const utils = require('./testUtils');

describe('Form Comments', () => {

    it ('should blah blah', () => {
        browser.get("");
        utils.mustBeLoggedInAs("ludetc", "Aa!!!000");
    });


});