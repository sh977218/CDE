<h4>Concepts</h4>
<div ng-controller="SaveCdeCtrl">
<div ng-show="cde.dataElementConcept.concepts.length > 0">
<h5>Data Element</h5>
<table class="table-bordered table-condensed table-striped">
    <thead>
        <tr>
            <th>Code Name</th>
            <th>Code System</th>
            <th>Code ID</th>
            <th id="conceptActions" ng-show="isAllowed(cde)"></th>
        </tr>
    </thead>
    <tbody>
        <tr ng-repeat="concept in cde.dataElementConcept.concepts">
            <td>{{concept.name}}</td>
            <td>{{concept.origin}}</td>
            <td>{{concept.originId}}</td>
            <td headers="actions" ng-show="isAllowed(cde)">
                 <i id="decConceptRemove-{{$index}}" class="fa fa-trash-o" ng-click="removeDecConcept($index)"></i>
            </td>
        </tr>
    </tbody>
</table>
</div>

<div ng-show="cde.objectClass.concepts.length > 0">
<h5>Class</h5>
<table class="table-bordered table-condensed table-striped">
    <thead>
        <tr>
            <th>Code Name</th>
            <th>Code System</th>
            <th>Code ID</th>
            <th id="conceptActions" ng-show="isAllowed(cde)"></th>
        </tr>
    </thead>
    <tbody>
        <tr ng-repeat="concept in cde.objectClass.concepts">
            <td>{{concept.name}}</td>
            <td>{{concept.origin}}</td>
            <td>{{concept.originId}}</td>
            <td headers="actions" ng-show="isAllowed(cde)">
                 <i id="ocConceptRemove-{{$index}}" class="fa fa-trash-o" ng-click="removeOcConcept($index)"></i>
            </td>
        </tr>
    </tbody>
</table>
</div>

<div ng-show="cde.property.concepts.length > 0">
<h5>Property</h5>
<table class="table-bordered table-condensed table-striped">
    <thead>
        <tr>
            <th>Code Name</th>
            <th>Code System</th>
            <th>Code ID</th>
            <th id="conceptActions" ng-show="isAllowed(cde)"></th>
        </tr>
    </thead>
    <tbody>
        <tr ng-repeat="concept in cde.property.concepts">
            <td>{{concept.name}}</td>
            <td>{{concept.origin}}</td>
            <td>{{concept.originId}}</td>
            <td headers="actions" ng-show="isAllowed(cde)">
                 <i id="propConceptRemove-{{$index}}" class="fa fa-trash-o" ng-click="removePropConcept($index)"></i>
            </td>
        </tr>
    </tbody>
</table>
</div>

<hr/>
    
<button class="btn btn-small" value="add" ng-click="openNewConcept()" id="addConcept"><i class="fa fa-plus"> </i> Add Concept</button>

<script type="text/ng-template" id="newConceptModalContent.html">
    <%- include newConceptModal.ejs %>
</script>
</div>
