<div class="modal-header ng-scope">
    <h4>Retire & Merge CDE</h4>
</div>     
<div class="modal-body">
    <div class="row">
        <div class="col-xs-10 col-xs-offset-1">
            <a target="_blank" href="#/deview?cdeId={{$parent.source._id}}">{{$parent.source.naming[0].designation}}</a> [{{ $parent.source.stewardOrg.name }}]
            <br><i class="fa fa-arrow-down"></i><br>
            <a target="_blank" href="#/deview?cdeId={{$parent.target._id}}">{{$parent.target.naming[0].designation}}</a> [{{ $parent.target.stewardOrg.name }}]
        </div>
    </div>
    <hr/>
    <div class="row">
        <div class="col-xs-10 col-xs-offset-1">
            Fields to be Imported
        <div class="row">
          <div class="col-md-offset-1 col-md-4"><input type="checkbox" ng-model="mergeRequest.fields.classifications"> Classifications</div>
        </div>             
        <div class="row">
          <div class="col-md-offset-1 col-md-4"><input type="checkbox" ng-model="mergeRequest.fields.ids"> Identifiers</div>
        </div>
        <div class="row">
          <div class="col-md-offset-1 col-md-4"><input type="checkbox" ng-model="mergeRequest.fields.naming"> Names</div>
        </div>
        <div class="row">
          <div class="col-md-offset-1 col-md-4"><input type="checkbox" ng-model="mergeRequest.fields.properties"> Properties</div>
        </div>             
        <div class="row">
          <div class="col-md-offset-1 col-md-4"><input type="checkbox" ng-model="mergeRequest.fields.attachments"> Attachments</div>
        </div>            
        </div>
    </div>
</div>

<div  ng-show="showApprovalAlert()" class="alert alert-warning">
    <div class=""><i class="fa fa-exclamation-triangle"></i> The action will require an approval.</div>
</div>

<div class="modal-footer">
    <button class="btn btn-default cancel" ng-click="cancel()">Cancel</button>
    <button class="btn btn-warning cancel" ng-click="sendMergeRequest()" id="sendMergeRequest">Retire & Merge</button>
</div>



